#include <stdio.h>
#include "main.h"

int main()
{
	/*	char **arrays = NULL;

	arrays = malloc(sizeof(char *) * 3);

	arrays[0] = malloc(sizeof(char) * 10);
	arrays[1] = malloc(sizeof(char) * 10);
	arrays[2] = malloc(sizeof(char) * 10);

	arrays[0] = "Hola com1";
	arrays[1] = "Hola com2";
	arrays[2] = "Hola com3";

	free(arrays[0]);
	free(arrays[1]);
	free(arrays[2]);
*/
	char *hola;

	hola = malloc(sizeof(char *));
	new_arrays[0] = hola;

    hola = "hola";

	free(new_arrays[0]);

	return (0);
}
